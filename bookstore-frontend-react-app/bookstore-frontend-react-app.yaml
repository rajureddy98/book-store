apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookstore-frontend-react-app
  labels:
    app: front-end
spec:
  replicas: 1
  selector:
    matchLabels:
      app: front-end
  template:
    metadata:
      labels:
        app: front-end
    spec:
      containers:
      - name: bookstore-frontend-react-app
        image: rajureddy98/bookstore-frontend-react-app:1.0
        ports:
          - containerPort: 3000
        imagePullPolicy: Always
---
apiVersion: v1
kind: Service
metadata:
  name: front-end-service
  labels:
    app: front-end-service
spec:
  type: LoadBalancer
  selector:
    app: front-end
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
