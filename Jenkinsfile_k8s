pipeline {
    agent any
    
    environment {
        msname = "${params.microservice}"
    }
    stages {
        stage('ssh'){
            steps{
                    sh 'kubectl get pods'
            }
        }
    }
}
